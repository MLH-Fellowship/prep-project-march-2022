import { useEffect, useState } from "react";

const Result = (results) => {
  console.log(results);
  // return <div>{results.results.cod}</div>;
  return <div>{results.results.cod}</div>;
};

export default Result;
